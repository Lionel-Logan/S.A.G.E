version: '3.8'

services:
  object-detection:
    build: .
    ports:
      - "8001:8001"
    environment:
      - OD_HOST=0.0.0.0
      - OD_PORT=8001
      - DEBUG=False
      - LOG_LEVEL=INFO
    volumes:
      - ./models:/app/models
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/api/v1/objects/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
